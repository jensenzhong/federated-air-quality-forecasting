# 实施计划与门禁

## 工作分解

| 阶段 | 输入 | 实施 | 输出 | 门禁 |
|---|---|---|---|---|
| M0 | 旧项目 | 哈希、归档、精确删除、脱敏 | 旧项目归档与删除清单 | 密钥扫描 0 命中；目标 CSV 不存在 |
| M1 | 项目章程 | 包结构、依赖、配置、文档、Git | 可安装仓库 | install/ruff/mypy/pytest 通过 |
| M2 | 12 个 PRSA CSV | 校验、因果插补、联邦缩放、窗口缓存 | manifest、metadata、质量表 | 12站/420768行；泄漏测试通过 |
| M3 | M2 缓存 | Naive、Local、Centralized、GRU/MLP | 基线运行 | 同切分同指标；模型冻结前不测 test |
| M4 | M3 模型 | ServerApp/ClientApp、4 固定策略、严格顺序运行时 | Flower 运行 | 2站 smoke；12站顺序1轮等价性；3轮资源基准 |
| M5 | M4 | Rule/LLM/Coordinator、缓存、预算回放 | MAS 运行 | Schema/边界/回放/测试隔离通过 |
| M6 | 冻结协议 | 主实验、消融、压力场景、统计 | validated 注册表 | 运行完整、资源和统计审计通过 |
| M7 | M6 | 自动表图、限制、主张映射 | 论文证据包 | 每项结论有运行 ID 与统计证据 |

## 配置与变更流程

1. 提案写入 `07_decision_log.md`，说明问题、候选方案、影响与回滚方式。
2. 对影响数据/模型/指标/统计的变更创建新配置或新运行 ID，不修改旧运行。
3. 先补测试，再实现；通过阶段检查后更新 `PROJECT_STATUS.md` 与 `ccfa.yaml`。
4. 协议冻结后，仅允许修复明确实现错误；任何实质方法变化视为新实验族。

## 2026-08-17 后续工作包

| 顺序 | 工作包 | 主要输出 | 通过条件 | 失败处理 |
|---|---|---|---|---|
| W1 | 严格低内存 Flower 运行时 | 顺序 Grid/runner、崩溃 invalidation、CLI、测试 | 调用真实 ClientApp 与 Strict Strategy；支持 FedAvg/FedProx/QFedAvg/FedAdam/Rule-MAS；禁止静默回退 | 保留当前 smoke 为 nonformal，M4 不通过 |
| W2 | 等价性与资源门禁 | 2 客户端合成对照、12 客户端一轮、3 轮资源报告 | 数组/指标容差通过；12 个 partition 唯一完整；记录峰值内存、单轮时间和字节量 | 修复运行时，不进入 screening |
| W3 | M3 参数筛选 | 4 个 GRU + 9 个固定联邦策略筛选结果、选择日志、配置哈希 | 全部只使用验证集；13 项队列完整；选择规则在看 test 前登记 | 缺项重跑，不冻结架构 |
| W4 | M5 MAS 门禁 | Rule-MAS、缓存 LLM、真实 LLM、决策回放、预算匹配轨迹 | Schema/边界/完整客户端/回放一致；正式 LLM 无 fallback | Rule-MAS、预算回放和严格 LLM-MAS 已通过 3 轮门禁及 30 轮 nonformal 确认；LLM 30 轮来源为 24 llm/4 rule/2 cache/0 fallback；保留资源和主张风险 |
| W5 | 单种子全流程与协议冻结 | seed 42 的 30 轮完整运行、预计总时长、冻结哈希 | FedProx μ=0.001、集中式 GRU、FedAdam、Rule-MAS、budget replay、严格 LLM-MAS 和 D025 修正后的 QFedAvg q=0.1/qffl_lr=1.0 的 30 轮验证工件已生成；集中式 runner 已完成进程级峰值 RSS 遥测；旧 QFedAvg 确认仍按实现缺陷隔离 | 完成确认工件审计并冻结协议哈希；协议冻结前不读取 test |
| W6 | 正式证据生成 | 5 种子主实验、消融、3 类鲁棒性、统计 | validated 注册表、配对键一致、Bootstrap/Holm 完整 | 无效运行按同配置重跑 |
| W7 | 论文证据包 | 表图、主张映射、限制、审计与投稿检查 | 每个数字追溯到 validated 运行 ID；完整性审计通过 | 返回对应实验/统计阶段修复 |

当前状态：v1 的 W1--W5 与 seed42 test 已完成，但 LLM-MAS 未超过预算匹配 FedProx，且其动作空间塌缩。原 W6 多 seed 队列暂停；项目进入 v2 方法重设计。

## 2026-08-17 v2 工作包

| 顺序 | 工作包 | 输出 | 通过条件 |
|---|---|---|---|
| V2-W1 | 状态与动作契约 | client state capsule、候选动作 Schema、隐私字段测试 | 不含原始行/逐时序列；非法动作 fail-closed |
| V2-W2 | Probe 与安全执行 | 固定预算 shadow probe、预算会计、trust region、FedProx fallback | 同候选可回放；拒绝/接受理由完整 |
| V2-W3 | 非 LLM 对照 | rule、contextual bandit、one-step probe oracle | 与 LLM 使用完全相同动作/探针预算 |
| V2-W4 | LLM proposer 与记忆 | 诊断、候选、证据字段、后验收益记忆 | 失败运行 invalid；LLM 不直接绕过执行器 |
| V2-W4b | 隐私传输与协调 | 客户端本地 agent、Flower SecAgg+、群组向量、聚合级协调器 | 合成四阶段闭环通过；服务器工件无客户端字段；test 继续阻断 |
| V2-W5 | P0/P1/P2 | 合成测试、3轮 smoke、10轮 seed42 val gate | v2 优于 v1，且达到预登记精度/尾部门槛 |
| V2-W6 | 新协议冻结与确认 | 新数据 manifest/哈希、多 seed、统计 | 只在 V2-W5 通过后启动 |

V2-W1--V2-W4b 的代码与合成门禁已通过；当前最高优先级是 12 站 P1 隐私 smoke。尚未启动真实训练；正式 test、安全评估聚合和机构隔离部署仍被阻断。

## 完成定义

每项实现需同时满足：代码存在、配置可解析、测试覆盖关键边界、真实命令可执行、产物字段齐全、文档与代码一致。只完成代码而未取得阶段门禁证据时状态保持“进行中”。

## 资源和预计时长治理

先测 1 轮峰值内存，再测 3 轮稳定时长。记录 CPU、可用内存、每客户端耗时与字节量。允许降低并发度，但不得降低客户端数、轮数、本地训练预算或更改聚合语义。资源门槛由 W2 实测确定；若顺序执行仍不可行，保留可运行工程并将 M4/M6 标记阻塞。

## 交付顺序

安全和数据正确性优先于模型性能；基线优先于复杂方法；Rule-MAS 优先于 LLM-MAS；单种子正确性优先于多种子队列；validated 结果优先于报告排版。

## 防偏航约束

- 协议冻结前只使用训练集/验证集；W1–W5 禁止读取测试结果做选择。
- 不因某次 smoke 指标好坏更换主指标、非劣界、压力强度或主要比较对象。
- 不把并发度变化描述为算法创新；论文只报告其资源影响和复现方式。
- 在主线完成前不增加多步预测、多目标、其他城市、隐私算法或前端平台。
